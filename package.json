{
  "name": "cf-saturn",
  "version": "0.1.1",
  "description": "Wiki-like static content generator",
  "main": "dist/lib/main.js",
  "types": "dist/lib/main.d.ts",
  "author": "Mgen (https://www.mgenware.com)",
  "homepage": "https://github.com/mgenware/cf-saturn",
  "bugs": "https://github.com/mgenware/cf-saturn/issues",
  "keywords": [
    "static site"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/mgenware/cf-saturn.git"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/bluebird": "^3.5.20",
    "@types/jest": "^22.2.3",
    "@types/node": "9.6.6",
    "@types/tmp": "^0.0.33",
    "copyfiles": "^2.0.0",
    "fx45-node": "^1.1.0",
    "jest": "^22.4.3",
    "rimraf": "^2.6.2",
    "tmp": "^0.0.33",
    "tslint": "^5.9.1",
    "typescript": "2.8.3"
  },
  "scripts": {
    "test": "yarn run clean && yarn run compile && yarn run lint && yarn run dev_test",
    "dev_test": "yarn run copy && yarn run t",
    "dt": "yarn run dev_test",
    "t": "jest",
    "build": "yarn test",
    "compile": "tsc",
    "dev": "yarn run clean && tsc -w",
    "prepublishOnly": "yarn run test",
    "lint": "tslint --project tsconfig.json --config tslint.json",
    "copy": "copyfiles \"tests/data/**/*.*\" \"dist\"",
    "clean:dist": "rimraf dist",
    "clean": "npm run clean:dist"
  },
  "engines": {
    "node": ">=8.0.0"
  },
  "dependencies": {
    "barbary-node": "^1.2.1",
    "bluebird": "^3.5.1",
    "escape-html": "^1.0.3",
    "fx43": "^2.1.0",
    "fx54-node": "^1.0.5",
    "lodash.trimend": "^4.5.1",
    "m-fs": "^1.0.3",
    "markdown-it-coldmark": "^0.0.7",
    "node-rename-path": "^2.0.2"
  },
  "files": [
    "dist/lib",
    ".gitignore",
    "README.md",
    "package.json"
  ]
}
